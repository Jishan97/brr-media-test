import { Grid, Typography } from "@mui/material";
import React from "react";

const Header = () => {
  return (
    <Grid>
      <Typography variant="h4">Uk Public Holidays 2022</Typography>
    </Grid>
  );
};

export default Header;
